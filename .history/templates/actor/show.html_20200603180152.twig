{% extends "base.html.twig" %}
{% block title %}Welcome!{% endblock %}
{% block body %}
    
    <h1>Actors</h1>
    <a href="{{ path('category_add') }}"><p>Ajouter une catégorie =></p></a>
    {% for actor in actors.programs %}
        <div>
            <h2>{{ loop.index }} / {{ actors.name }}</h2></a>
            <div class="d-flex">
                <img class="align-self-start mr-3 col-1" src="{{ program.poster }}" alt="{{ program.title }} poster">
                <p>{{ program.synopsis }}</p>
            </div>
        </div>
    {% else %}
        <h3>Aucune série trouvée.</h3>
    {% endfor %}
    <div class="d-flex flex-row-reverse">
        <a href="{{ path('app_index') }}">
            Retour à l'accueil
        </a>
    </div>

{% endblock %}
{% block javascripts %}{% endblock %}