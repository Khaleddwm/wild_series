<nav class="navbar navbar-light bg-light">
  <form class="form-inline">
    <a class="btn btn-outline-danger" type="button" href="{{ path('app_index') }}">Accueil</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('wild_index') }}">Wild Series</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('wild_show', { 'slug': "walking-dead" }) }}">Show slug</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('wild_show_category', { 'categoryName': "horreur" }) }}">Show Program Category</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('wild_show_program', { 'programTitle': "walking-dead" }) }}">Show Program</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('wild_show_season', { 'seasonId': "1" }) }}">Show season</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('wild_show_episode', { 'id': "1" }) }}">Show episode</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('category_add') }}">Ajouter catégorie</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('program_index') }}">Program</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('season_index') }}">Season</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('episode_index') }}">Episode</a>
    <a class="btn btn-outline-primary" type="button" href="{{ path('actor_index') }}">Actor</a>
  </form>
</nav>